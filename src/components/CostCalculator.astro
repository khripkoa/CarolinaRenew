---
// CostCalculator - Interactive cost estimator to engage users
---

<section class="py-16 bg-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-10">
            <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
                Get an Instant Estimate
            </h2>
            <p class="text-lg text-gray-600">
                Answer a few quick questions to see your project's estimated
                cost range.
            </p>
        </div>

        <div
            x-data="{
        step: 1,
        service: '',
        size: '',
        quality: '',
        estimate: { min: 0, max: 0 },
        services: [
          { id: 'cabinet', name: 'Cabinet Refinishing', icon: 'cabinet', base: 2500 },
          { id: 'interior', name: 'Interior Painting', icon: 'interior', base: 1800 },
          { id: 'exterior', name: 'Exterior Painting', icon: 'exterior', base: 3500 },
          { id: 'flooring', name: 'Floor Refinishing', icon: 'flooring', base: 2200 },
          { id: 'storage', name: 'Storage Solutions', icon: 'storage', base: 2000 }
        ],
        sizes: [
          { id: 'small', name: 'Small (1 room)', multiplier: 0.5 },
          { id: 'medium', name: 'Medium (2-3 rooms)', multiplier: 1.0 },
          { id: 'large', name: 'Large (4+ rooms or full house)', multiplier: 1.8 }
        ],
        qualities: [
          { id: 'standard', name: 'Standard Quality', multiplier: 1.0 },
          { id: 'premium', name: 'Premium Quality', multiplier: 1.4 }
        ],
        calculate() {
          const selectedService = this.services.find(s => s.id === this.service);
          const selectedSize = this.sizes.find(s => s.id === this.size);
          const selectedQuality = this.qualities.find(q => q.id === this.quality);
          
          if (selectedService && selectedSize && selectedQuality) {
            const base = selectedService.base * selectedSize.multiplier * selectedQuality.multiplier;
            this.estimate.min = Math.round(base * 0.85);
            this.estimate.max = Math.round(base * 1.15);
          }
        }
      }"
            class="bg-gray-50 rounded-2xl shadow-xl p-6 md:p-10"
        >
            <!-- Progress Bar -->
            <div class="mb-8" x-show="step <= 3">
                <div class="flex justify-between mb-2">
                    <span class="text-sm font-medium text-gray-700"
                        >Progress</span
                    >
                    <span
                        class="text-sm font-medium text-gray-700"
                        x-text="`Step ${step} of 3`"></span>
                </div>
                <div class="w-full bg-gray-200 rounded-full h-2">
                    <div
                        class="bg-gradient-to-r from-blue-600 to-indigo-600 h-2 rounded-full transition-all duration-300"
                        :style="`width: ${Math.min((step / 3) * 100, 100)}%`"
                    >
                    </div>
                </div>
            </div>

            <!-- Step 1: Service Selection -->
            <div x-show="step === 1" x-cloak>
                <h3 class="text-xl font-bold text-gray-900 mb-6">
                    What service do you need?
                </h3>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                    <template x-for="svc in services" :key="svc.id">
                        <button
                            @click="service = svc.id; step = 2"
                            class="p-6 border-2 rounded-xl text-left transition hover:border-blue-500 hover:shadow-lg"
                            :class="service === svc.id ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                        >
                            <div class="w-12 h-12 mb-3 text-blue-600">
                                <!-- Cabinet Icon -->
                                <svg
                                    x-show="svc.icon === 'cabinet'"
                                    class="w-12 h-12"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="1.5"
                                        d="M5 3h14a2 2 0 012 2v14a2 2 0 01-2 2H5a2 2 0 01-2-2V5a2 2 0 012-2zm0 9h14M9 3v18m6-18v18"
                                    ></path>
                                </svg>
                                <!-- Interior Icon -->
                                <svg
                                    x-show="svc.icon === 'interior'"
                                    class="w-12 h-12"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="1.5"
                                        d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414a1 1 0 00-.707-.293H7a2 2 0 00-2 2v14a2 2 0 002 2z"
                                    ></path>
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="1.5"
                                        d="M9 17h6M9 13h6M9 9h2"></path>
                                </svg>
                                <!-- Exterior Icon -->
                                <svg
                                    x-show="svc.icon === 'exterior'"
                                    class="w-12 h-12"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="1.5"
                                        d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
                                    ></path>
                                </svg>
                                <!-- Flooring Icon -->
                                <svg
                                    x-show="svc.icon === 'flooring'"
                                    class="w-12 h-12"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="1.5"
                                        d="M4 5a1 1 0 011-1h4a1 1 0 011 1v4a1 1 0 01-1 1H5a1 1 0 01-1-1V5zm10 0a1 1 0 011-1h4a1 1 0 011 1v4a1 1 0 01-1 1h-4a1 1 0 01-1-1V5zM4 15a1 1 0 011-1h4a1 1 0 011 1v4a1 1 0 01-1 1H5a1 1 0 01-1-1v-4zm10 0a1 1 0 011-1h4a1 1 0 011 1v4a1 1 0 01-1 1h-4a1 1 0 01-1-1v-4z"
                                    ></path>
                                </svg>
                                <!-- Storage Icon -->
                                <svg
                                    x-show="svc.icon === 'storage'"
                                    class="w-12 h-12"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="1.5"
                                        d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4"
                                    ></path>
                                </svg>
                            </div>
                            <div
                                class="font-bold text-gray-900"
                                x-text="svc.name"
                            >
                            </div>
                        </button>
                    </template>
                </div>
            </div>

            <!-- Step 2: Size Selection -->
            <div x-show="step === 2" x-cloak>
                <h3 class="text-xl font-bold text-gray-900 mb-6">
                    What's the project size?
                </h3>
                <div class="space-y-3">
                    <template x-for="sz in sizes" :key="sz.id">
                        <button
                            @click="size = sz.id; step = 3"
                            class="w-full p-5 border-2 rounded-xl text-left transition hover:border-blue-500 hover:shadow-lg flex items-center justify-between"
                            :class="size === sz.id ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                        >
                            <span
                                class="font-semibold text-gray-900"
                                x-text="sz.name"></span>
                            <svg
                                class="w-6 h-6 text-blue-600"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M9 5l7 7-7 7"></path>
                            </svg>
                        </button>
                    </template>
                </div>
                <button
                    @click="step = 1"
                    class="mt-6 text-gray-600 hover:text-gray-900 font-medium"
                >
                    ← Back
                </button>
            </div>

            <!-- Step 3: Quality Selection -->
            <div x-show="step === 3" x-cloak>
                <h3 class="text-xl font-bold text-gray-900 mb-6">
                    Choose your finish quality:
                </h3>
                <div class="space-y-3">
                    <template x-for="qual in qualities" :key="qual.id">
                        <button
                            @click="quality = qual.id; calculate(); step = 4"
                            class="w-full p-5 border-2 rounded-xl text-left transition hover:border-blue-500 hover:shadow-lg flex items-center justify-between"
                            :class="quality === qual.id ? 'border-blue-500 bg-blue-50' : 'border-gray-200'"
                        >
                            <span
                                class="font-semibold text-gray-900"
                                x-text="qual.name"></span>
                            <svg
                                class="w-6 h-6 text-blue-600"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M9 5l7 7-7 7"></path>
                            </svg>
                        </button>
                    </template>
                </div>
                <button
                    @click="step = 2"
                    class="mt-6 text-gray-600 hover:text-gray-900 font-medium"
                >
                    ← Back
                </button>
            </div>

            <!-- Step 4: Results -->
            <div x-show="step === 4" x-cloak class="text-center">
                <div class="mb-6">
                    <div
                        class="inline-flex items-center justify-center w-16 h-16 bg-green-100 rounded-full mb-4"
                    >
                        <svg
                            class="w-8 h-8 text-green-600"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M5 13l4 4L19 7"></path>
                        </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-900 mb-2">
                        Your Estimated Cost Range
                    </h3>
                    <div class="text-5xl font-bold text-blue-600 my-6">
                        $<span x-text="estimate.min.toLocaleString()"></span> - $<span
                            x-text="estimate.max.toLocaleString()"></span>
                    </div>
                    <p class="text-gray-600 max-w-md mx-auto mb-8">
                        This is a rough estimate based on your selections.
                        Actual costs may vary based on specific project details,
                        materials, and labor.
                    </p>
                </div>

                <div
                    class="bg-blue-50 border border-blue-200 rounded-xl p-6 mb-6"
                >
                    <p class="font-semibold text-gray-900 mb-3">
                        Want an exact quote?
                    </p>
                    <a
                        href="tel:+19804088122"
                        class="inline-block w-full md:w-auto px-8 py-4 bg-gradient-to-r from-blue-600 to-indigo-600 text-white font-bold rounded-lg hover:from-blue-700 hover:to-indigo-700 transform hover:scale-105 transition duration-200 shadow-lg hover:shadow-xl mb-3"
                    >
                        📞 Call (980) 408-8122
                    </a>
                    <p class="text-sm text-gray-600">
                        Or scroll down to fill out our contact form
                    </p>
                </div>

                <button
                    @click="step = 1; service = ''; size = ''; quality = ''; estimate = {min: 0, max: 0}"
                    class="text-gray-600 hover:text-gray-900 font-medium"
                >
                    ← Start Over
                </button>
            </div>
        </div>
    </div>
</section>

<style>
    [x-cloak] {
        display: none !important;
    }
</style>
